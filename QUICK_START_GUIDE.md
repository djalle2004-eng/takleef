# 🚀 دليل البدء السريع - Takleef

## 📖 مقدمة
هذا دليل سريع للبدء بالعمل على مشروع Takleef - نظام إدارة التكليفات الجامعية.

---

## 📚 التحليل الشامل

تم إنشاء تحليل شامل للمشروع في 5 ملفات:

| الملف | المحتوى |
|------|---------|
| **ANALYSIS_OVERVIEW.md** | نظرة عامة، إحصائيات، البنية الأساسية |
| **ANALYSIS_TECHNICAL.md** | Stack التقني، الأدوات، الإعدادات |
| **ANALYSIS_DATABASE.md** | قاعدة البيانات، الجداول، العلاقات، الاستعلامات |
| **ANALYSIS_FEATURES.md** | الميزات والوحدات الوظيفية بالتفصيل |
| **ANALYSIS_STATUS.md** | حالة المشروع، نسبة الإنجاز، الخطط المستقبلية |

**ابدأ بقراءة:** `ANALYSIS_OVERVIEW.md`

---

## ⚡ البدء السريع (5 دقائق)

### 1. التثبيت
```bash
cd c:\Users\Ali\Documents\takleef
npm install
```

### 2. إعداد البيئة
أنشئ ملف `.env`:
```env
DATABASE_URL=your_neon_postgresql_url
JWT_SECRET=your_secret_key_min_32_chars
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. تهيئة قاعدة البيانات
```bash
# ابدأ السيرفر
npm run dev

# في المتصفح، قم بزيارة:
http://localhost:3000/api/init-db
http://localhost:3000/api/init-admin
```

### 4. تسجيل الدخول
```
URL: http://localhost:3000/signin

Admin:
  Email: hussain-ali@univ-eloued.dz
  Password: Aida@miral1981**
```

---

## 🗺️ خريطة المشروع

### الهيكل الأساسي
```
takleef/
├── 📁 app/                    # Next.js App Router
│   ├── 📁 admin/             # لوحة الإدارة
│   ├── 📁 dashboard/         # لوحة الأساتذة
│   ├── 📁 api/               # API Routes (40+ endpoint)
│   └── 📁 signin, signup, etc.
│
├── 📁 components/            # React Components (11 مكون)
│   ├── AdminSidebar.tsx
│   ├── TeachingPreferences.tsx
│   └── ...
│
├── 📁 lib/                   # Utilities
│   ├── db.ts                 # قاعدة البيانات
│   ├── auth.ts               # المصادقة
│   └── validations.ts        # Zod Schemas
│
├── 📁 messages/              # الترجمة
│   ├── ar.json              # العربية (300+ keys)
│   └── en.json              # الإنجليزية
│
├── middleware.ts             # حماية المسارات
├── i18n.ts                   # إعداد الترجمة
└── 📄 32+ ملف .md           # التوثيق
```

### المسارات الرئيسية

#### للأساتذة
```
/signin                       → تسجيل الدخول
/dashboard                    → الصفحة الرئيسية
/dashboard/teaching-preferences → اختيار الرغبات
/dashboard/reports            → التقارير
/complete-profile             → إكمال الملف الشخصي
```

#### للإدارة
```
/admin                        → Dashboard
/admin/academic-years         → السنوات الأكاديمية
/admin/modules                → المقاييس (هرمي)
/admin/users                  → المستخدمين
/admin/preferences            → عرض الرغبات
/admin/reports                → التقارير المتقدمة
```

---

## 🔑 المفاهيم الأساسية

### 1. البنية الهرمية
```
Department (5 أقسام ثابتة)
    ↓
Specialty (جذع مشترك، ليسانس، ماستر)
    ↓
Module (المقاييس)
```

### 2. نظام التفضيلات
```typescript
Professor Preference {
  module_id: number;        // المقياس
  teaching_type:            // نوع التدريس
    'LECTURE'   // محاضرات
    | 'TUTORIAL'  // أعمال موجهة
    | 'BOTH';     // كلاهما
  priority_level: 1-5;      // الأولوية (1 = أعلى)
}
```

### 3. السنوات والفصول
```
Academic Year (2024-2025)
  ├── Semester 1 (إنشاء تلقائي)
  └── Semester 2 (إنشاء تلقائي)
```

---

## 🎯 المهام الشائعة

### إضافة سنة أكاديمية جديدة
```typescript
// 1. في لوحة الإدارة
/admin/academic-years → "Add New Year"

// 2. يتم إنشاء فصلين تلقائياً
// 3. يمكن ربط المقاييس بالفصول
```

### استيراد أساتذة من Excel
```typescript
// 1. تحميل القالب
/admin/users → "Import Professors" → "Download Template"

// 2. ملء البيانات في Excel
// 3. رفع الملف
// 4. سيتم إنشاء الحسابات تلقائياً بكلمة مرور: Professor@123
```

### إنشاء تقرير
```typescript
// 1. اذهب إلى
/dashboard/reports (للأساتذة)
/admin/reports (للإدارة)

// 2. اختر نوع التقرير
// 3. حدد المرشحات (اختياري)
// 4. انقر "Generate Report"
// 5. تصدير (Excel/PDF)
```

### تغيير اللغة
```typescript
// انقر على زر "EN" أو "عربي" في:
- أعلى يمين صفحة تسجيل الدخول
- شريط التنقل (Dashboard)
- رأس الشريط الجانبي (Admin)
```

---

## 🔧 التطوير

### إضافة ميزة جديدة

#### 1. إنشاء API Route
```typescript
// app/api/my-feature/route.ts
import { sql } from '@neondatabase/serverless';
import { NextResponse } from 'next/server';

export async function GET() {
  const data = await sql`SELECT * FROM my_table`;
  return NextResponse.json({ data: data.rows });
}
```

#### 2. إنشاء صفحة
```typescript
// app/my-page/page.tsx
export default async function MyPage() {
  const data = await fetch('/api/my-feature');
  return <div>{/* UI */}</div>;
}
```

#### 3. إضافة ترجمات
```json
// messages/ar.json
{
  "mySection": {
    "title": "عنوان",
    "button": "زر"
  }
}

// messages/en.json
{
  "mySection": {
    "title": "Title",
    "button": "Button"
  }
}
```

```typescript
// في المكون
import { useTranslations } from 'next-intl';

const t = useTranslations('mySection');
return <h1>{t('title')}</h1>;
```

---

## 📊 قاعدة البيانات

### الجداول الرئيسية
```sql
users                  -- المصادقة
professors             -- معلومات الأساتذة
academic_years         -- السنوات الأكاديمية
semesters              -- الفصول (2 لكل سنة)
departments            -- الأقسام (5 ثابتة)
specialties            -- التخصصات
modules                -- المقاييس
preferences            -- رغبات الأساتذة
password_reset_tokens  -- استعادة كلمة المرور
```

### الاستعلامات الشائعة
```sql
-- جلب ملف أستاذ
SELECT p.* FROM professors p
JOIN users u ON p.user_id = u.id
WHERE u.id = ?;

-- جلب مقاييس قسم معين
SELECT m.* FROM modules m
JOIN specialties s ON m.specialty_id = s.id
WHERE s.department_id = ?;

-- جلب رغبات أستاذ
SELECT p.*, m.module_name
FROM preferences p
JOIN modules m ON p.module_id = m.id
WHERE p.professor_id = ?;
```

---

## 🐛 حل المشاكل الشائعة

### "Cannot find module lucide-react"
```bash
npm install lucide-react
```

### "Database connection error"
```bash
# تحقق من:
1. DATABASE_URL في .env صحيح
2. Neon database متصل
3. IP address مسموح في Neon
```

### "401 Unauthorized"
```bash
# السبب: Token غير صالح أو منتهي
# الحل:
1. امسح الكوكيز
2. سجل دخول مرة أخرى
```

### "No data in reports"
```bash
# السبب: لا توجد بيانات أو مرشحات خاطئة
# الحل:
1. تأكد من وجود رغبات محفوظة
2. تحقق من المرشحات (السنة، القسم)
3. افحص Console للأخطاء
```

---

## 📖 التوثيق الإضافي

### للمطورين الجدد
1. اقرأ `ANALYSIS_OVERVIEW.md` للفهم العام
2. راجع `ANALYSIS_TECHNICAL.md` للـ Stack
3. افهم `ANALYSIS_DATABASE.md` للبيانات

### للميزات المحددة
- **Admin Setup**: `ADMIN_SETUP.md`
- **CRUD Features**: `CRUD_FEATURES.md`
- **Reports System**: `REPORTS_SYSTEM.md`
- **i18n System**: `LANGUAGE_SYSTEM_READY.md`
- **Hierarchical Modules**: `HIERARCHICAL_MODULES.md`

### للإصلاحات السابقة
- `ALL_REPORTS_FIX_SUMMARY.md`
- `API_ENDPOINTS_PUBLIC_FIX.md`

---

## ✅ Checklist للمساهمين

قبل عمل Pull Request:

- [ ] الكود يعمل بدون أخطاء TypeScript
- [ ] تم اختبار الميزة محلياً
- [ ] الكود متبع لـ Coding Style الموجود
- [ ] تم إضافة ترجمات (عربي + إنجليزي)
- [ ] تم تحديث التوثيق إن لزم الأمر
- [ ] لا توجد console.logs غير ضرورية
- [ ] تم استخدام Zod للتحقق من المدخلات
- [ ] الاستعلامات SQL محمية (parameterized)

---

## 🎓 الموارد المفيدة

### للمشروع
- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [next-intl](https://next-intl-docs.vercel.app/)
- [Zod](https://zod.dev/)

### لقاعدة البيانات
- [Neon Docs](https://neon.tech/docs)
- [PostgreSQL Tutorial](https://www.postgresql.org/docs/)

### للأمان
- [JWT Best Practices](https://jwt.io/introduction)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)

---

## 📞 الدعم

### للأسئلة
1. راجع ملفات `.md` الموجودة (32 ملف)
2. افحص الكود المشابه في المشروع
3. راجع Console للأخطاء

### للمشاكل التقنية
1. تحقق من `ANALYSIS_STATUS.md` → "المشاكل المعروفة"
2. افحص GitHub Issues (إن وجد)

---

## 🎯 الخطوات التالية

### للمطور الجديد
1. ✅ قرأت هذا الدليل
2. ✅ ثبّت المشروع محلياً
3. ✅ أنشأت قاعدة البيانات
4. ✅ سجلت دخول كمدير
5. ⏭️ استكشف لوحة الإدارة
6. ⏭️ جرّب إنشاء بيانات تجريبية
7. ⏭️ اقرأ `ANALYSIS_FEATURES.md`
8. ⏭️ ابدأ بمهمتك الأولى!

### للإدارة
1. ✅ المشروع مثبت ويعمل
2. ⏭️ استيراد بيانات الأساتذة من Excel
3. ⏭️ إنشاء الأقسام والتخصصات والمقاييس
4. ⏭️ إنشاء السنوات الأكاديمية
5. ⏭️ إبلاغ الأساتذة بالبدء

### للأستاذ
1. ✅ تسجيل دخول أول مرة
2. ✅ إكمال الملف الشخصي
3. ⏭️ اختيار رغبات التدريس
4. ⏭️ مراجعة التقارير

---

**مرحباً بك في مشروع Takleef! 🎓**

**نتمنى لك تجربة تطوير ممتعة! 🚀**
